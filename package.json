{
  "name": "realestate-monorepo",
  "private": true,
  "scripts": {
    "dev:up": "docker compose -f docker/compose/docker-compose.yml up --build",
    "dev:down": "docker compose -f docker/compose/docker-compose.yml down -v"
  }
}
